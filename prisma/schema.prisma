generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Msg {
  id      Int    @id
  senttime DateTime @default(now())
  name String 
  message String
  stamp Int
}
